services:
  - type: web
    name: final-dsa-sheet
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false # The Neon Database URL to be provided in the Render UI
